function preload(){
seaImg = loadImage("sea.pag");
shipImg1 =loadAnimation (//enlista los nombres de los png que desees);
}

function setup(){
spriteName.addImage(seaImg);
createCanvas(400,400);
}

functioncdraw() {
//codigo parareiniciar el fondo.
if (sea.x <0) {
    sea.x=sea.wisth/2;
}
blackground("blue");

}